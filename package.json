{
  "name": "flick",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "node scripts/bump-extension-version.cjs && vite build",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "vite": "^7.2.2",
    "typescript": "^5.9.3"
  }
}
