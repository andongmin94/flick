{
  "name": "flick",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "node scripts/bump-extension-version.cjs && tsc --noEmit && vite build && node scripts/verify-extension.cjs"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.0.10"
  }
}
